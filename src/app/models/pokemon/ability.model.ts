export class Ability {
  name: string;
  effectDescription: string;

  constructor(name: string, effectDescription: string) {
    this.name = name;
    this.effectDescription = effectDescription;
  }
}
